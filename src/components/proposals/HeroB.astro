---
// Hero B: "Bold & Modern" — dark dramatic gradient with geometric accent shapes
---

<section class="relative min-h-screen flex items-center overflow-hidden bg-gradient-to-r from-[#1a2a2f] via-[#2E3F44] to-[#354a51]">
  <!-- Geometric Accent Shapes -->
  <div class="absolute inset-0 pointer-events-none overflow-hidden">
    <svg class="absolute top-0 right-0 w-2/3 h-full opacity-[0.07]" viewBox="0 0 600 900" fill="none" xmlns="http://www.w3.org/2000/svg">
      <!-- Large circle -->
      <circle cx="480" cy="200" r="180" stroke="#8B8F65" stroke-width="1.5" />
      <!-- Medium circle -->
      <circle cx="400" cy="500" r="250" stroke="#8B8F65" stroke-width="1" />
      <!-- Small circle -->
      <circle cx="520" cy="750" r="80" stroke="#8B8F65" stroke-width="1.5" />
      <!-- Rotated rectangle -->
      <rect x="350" y="650" width="120" height="120" rx="16" stroke="#8B8F65" stroke-width="1.5" transform="rotate(20 410 710)" />
      <!-- Diamond -->
      <path d="M550 400 L580 450 L550 500 L520 450 Z" stroke="#8B8F65" stroke-width="1" />
      <!-- Horizontal lines -->
      <line x1="300" y1="300" x2="600" y2="300" stroke="#8B8F65" stroke-width="0.5" opacity="0.5" />
      <line x1="350" y1="600" x2="600" y2="600" stroke="#8B8F65" stroke-width="0.5" opacity="0.5" />
    </svg>

    <!-- Subtle dot grid on left -->
    <svg class="absolute bottom-0 left-0 w-1/3 h-1/2 opacity-[0.04]" viewBox="0 0 300 400" fill="none" xmlns="http://www.w3.org/2000/svg">
      {Array.from({ length: 8 }).map((_, row) =>
        Array.from({ length: 6 }).map((_, col) => (
          <circle cx={30 + col * 50} cy={30 + row * 50} r="2" fill="white" />
        ))
      )}
    </svg>
  </div>

  <!-- Content (left-aligned) -->
  <div class="relative z-10 w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
    <div class="max-w-2xl">
      <!-- Accent Line -->
      <div class="w-16 h-1 bg-sageGreen rounded-full mb-8 animate-on-scroll"></div>

      <!-- Main Heading -->
      <h1 class="mb-6 animate-on-scroll">
        <span class="block text-warmWhite text-4xl sm:text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-extrabold leading-[0.95] tracking-tight">
          Omar Ibn
        </span>
        <span class="block text-warmWhite text-4xl sm:text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-extrabold leading-[0.95] tracking-tight">
          Al Khattab
        </span>
        <span class="block text-sageGreen-light text-4xl sm:text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-extrabold leading-[0.95] tracking-tight">
          Centre
        </span>
      </h1>

      <!-- Tagline -->
      <p class="text-warmWhite/70 text-base sm:text-lg md:text-xl max-w-lg mb-10 leading-relaxed animate-on-scroll">
        Building a vibrant future for Edmonton's Muslim community through faith, education, and service.
      </p>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row items-start gap-4 animate-on-scroll">
        <a
          href="/prayertimes"
          class="w-full sm:w-auto bg-warmWhite text-deepTeal hover:bg-softBeige font-bold px-8 py-4 rounded-lg transition-colors duration-200 text-base min-h-[44px] flex items-center justify-center"
        >
          View Prayer Times
        </a>
        <a
          href="/donate"
          class="w-full sm:w-auto bg-terracottaRed hover:bg-terracottaRed-darker text-white font-bold px-8 py-4 rounded-lg transition-colors duration-200 text-base min-h-[44px] flex items-center justify-center"
        >
          Donate Now
        </a>
      </div>
    </div>
  </div>
</section>
